"""Package namespace for category extension modules.

This module intentionally exports no runtime objects.  Keep package-level
initialization side-effect free so dynamic category discovery can import it
safely during tests, migrations, and application startup.
"""
