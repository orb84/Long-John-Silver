"""Focused domain model modules used behind ``src.core.models``.

The package keeps large Pydantic model groups separated by bounded context while
preserving the compatibility facade expected by legacy imports.
"""
