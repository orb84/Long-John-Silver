"""
Action handlers package for LJS.

Each module provides a handler class that groups related action
implementations for the ActionGateway. Handlers are instantiated
in the composition root (src/web/app.py) with their service dependencies.
"""
